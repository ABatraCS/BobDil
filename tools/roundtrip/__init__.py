"""Where the time goes between the driver's hand and the driver's hand.

`bobdil-kernel --trace` writes a `.bdtrace`; this reads it. The package is
named for the measurement rather than the mechanism because the mechanism --
spans, stamps, a ring -- is an implementation detail of answering one question
that architecture.md 5.6 poses and nothing else here answers.
"""
